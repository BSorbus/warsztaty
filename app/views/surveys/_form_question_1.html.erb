<fieldset class="my-fieldset">
  <legend class="my-fieldset"><%= t('surveys.question_1.fieldset_title') %></legend>

    <div class="form-group col-sm-12">
      <p><%= t('surveys.question_1.body') %></p>
    </div>
    <div class="control-group col-sm-12">
      <%= f.label :answer_1_place_1, class: "control-label" %>
      <%= f.select :answer_1_place_1, 
            Department.where.not(id: [1, 2, @current_user.department_id]).order(:short).collect {|p| [ "#{p.short}, #{p.name}", p.id ]},
            { include_blank: true, multiple: false }, 
            { class: 'form-control input-sm', disabled: input_disabled } %>
    </div>
    <div class="control-group col-sm-12">
      <%= f.label :answer_1_place_2, class: "control-label" %>
      <%= f.select :answer_1_place_2, 
            Department.where.not(id: [1, 2, @current_user.department_id]).order(:short).collect {|p| [ "#{p.short}, #{p.name}", p.id ]},
            { include_blank: true, multiple: false }, 
            { class: 'form-control input-sm', disabled: input_disabled } %>
    </div>
    <div class="control-group col-sm-12">
      <%= f.label :answer_1_place_3, class: "control-label" %>
      <%= f.select :answer_1_place_3, 
            Department.where.not(id: [1, 2, @current_user.department_id]).order(:short).collect {|p| [ "#{p.short}, #{p.name}", p.id ]},
            { include_blank: true, multiple: false }, 
            { class: 'form-control input-sm', disabled: input_disabled } %>
    </div>
</fieldset>
